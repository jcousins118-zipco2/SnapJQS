## Packages
framer-motion | Animation for result reveals and transitions
date-fns | Formatting timestamps in history
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The application requires a monospace font for JSON editing and hash display.
We assume the backend endpoint /api/canonicalize expects a JSON object wrapper { "data": ... }
